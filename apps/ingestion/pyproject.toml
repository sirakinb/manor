[project]
name = "manor-ingestion"
version = "0.1.0"
description = "Manor's deterministic pipeline runner (Zoho, Instagram, Buildium, listings, water bills, monthly recap)."
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
